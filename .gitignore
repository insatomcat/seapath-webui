__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
coverage.json
htmlcov/
*.egg-info/

# Claude Code keeps its worktrees and its local settings here. Both belong to
# a developer machine, and a worktree is a git repository of its own.
.claude/
