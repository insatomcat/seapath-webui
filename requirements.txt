# Copyright (C) 2026, RTE (http://www.rte-france.com)
# SPDX-License-Identifier: Apache-2.0

fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
pydantic==2.10.5
pydantic-settings==2.7.1

# Self signed certificate generation at first boot, and the SSH key pairs the
# trust is built on.
cryptography==44.0.0

# The inventory is YAML, and it is the product.
pyyaml==6.0.2

# Reading YAML with the line and column of every value, which is what lets a
# form save edit the two lines it changes and leave the other hundred and
# fifty, comments included, byte for byte. Used as a parser that reports
# positions; the writing is a splice into the original text.
ruamel.yaml==0.18.10

# The configuration plane. ansible-core is pinned to the version prepare.sh
# enforces in seapath-ansible: an image carrying a different one runs playbooks
# against a version they were never tested with.
ansible-core~=2.16.0
ansible-runner==2.4.1

# PAM authentication against the local Unix accounts. Imports as `pam`.
# `six` is an undeclared dependency of python-pam 2.0.2: without it the import
# fails, and a service that cannot import PAM refuses every password while
# looking like a machine whose operators all mistyped theirs.
python-pam==2.0.2
six==1.17.0
